services:
  user-service:
    build: ./UserService
    container_name: user-service
    ports:
      - "8081:8081"

  api-gateway:
    build: ./ApiGateway
    container_name: api-gateway
    ports:
      - "8080:8080"
    depends_on:
      - user-service
