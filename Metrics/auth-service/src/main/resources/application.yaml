spring:
  application:
    name: auth-service

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: always
    prometheus:
      enabled: true
  metrics:
    tags:
      service: ${spring.application.name}      # Tags every metric with the service name
    export:
      prometheus:
        enabled: true

logging:
  level:
    root: INFO
